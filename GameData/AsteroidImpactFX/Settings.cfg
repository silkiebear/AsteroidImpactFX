AsteroidImpactFX
{
    // Effect Settings
    EffectDuration = 120           // Duration of effects in seconds
    EffectScale = 1000            // Scale of the explosion effect
    LightRange = 25000            // Range of the light flash
    LightIntensity = 8            // Intensity of the light flash
    LightDuration = 3             // Duration of the light flash in seconds
    
    // Sound Settings
    EnableSound = true            // Whether to play impact sounds
    SoundRange = 50000            // Maximum distance to hear sounds
    SoundVolume = 1.0             // Maximum volume at impact site
    
    // Detection Settings
    LookAheadTime = 300           // How far ahead to look for impacts (seconds)
    MinAsteroidMass = 0.1         // Minimum asteroid mass to trigger effects (tons)
    
    // Debug Settings
    DebugLogging = false          // Enable detailed debug logging
} 